package com.example.mylibrary

class HelloWorld {
    companion object {
        // Static-like function inside a companion object
        fun sum(a: Int, b: Int): Int {
            return a + b
        }
    }
}
